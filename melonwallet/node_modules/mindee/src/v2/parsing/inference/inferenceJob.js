export class InferenceJob {
    constructor(serverResponse) {
        this.id = serverResponse["id"];
    }
    toString() {
        return ("Job\n" +
            "===\n" +
            `:ID: ${this.id}\n`);
    }
}

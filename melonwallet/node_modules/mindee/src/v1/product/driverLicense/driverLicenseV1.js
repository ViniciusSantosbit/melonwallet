import { Inference, Page } from "../../../v1/parsing/common/index.js";
import { DriverLicenseV1Document } from "./driverLicenseV1Document.js";
/**
 * Driver License API version 1 inference prediction.
 */
export class DriverLicenseV1 extends Inference {
    constructor(rawPrediction) {
        super(rawPrediction);
        /** The endpoint's name. */
        this.endpointName = "driver_license";
        /** The endpoint's version. */
        this.endpointVersion = "1";
        /** The document's pages. */
        this.pages = [];
        this.prediction = new DriverLicenseV1Document(rawPrediction["prediction"]);
        rawPrediction["pages"].forEach((page) => {
            if (page.prediction !== undefined && page.prediction !== null &&
                Object.keys(page.prediction).length > 0) {
                this.pages.push(new Page(DriverLicenseV1Document, page, page["id"], page["orientation"]));
            }
        });
    }
}

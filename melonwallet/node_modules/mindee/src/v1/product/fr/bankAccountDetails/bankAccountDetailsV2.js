import { Inference, Page } from "../../../../v1/parsing/common/index.js";
import { BankAccountDetailsV2Document } from "./bankAccountDetailsV2Document.js";
/**
 * Bank Account Details API version 2 inference prediction.
 */
export class BankAccountDetailsV2 extends Inference {
    constructor(rawPrediction) {
        super(rawPrediction);
        /** The endpoint's name. */
        this.endpointName = "bank_account_details";
        /** The endpoint's version. */
        this.endpointVersion = "2";
        /** The document's pages. */
        this.pages = [];
        this.prediction = new BankAccountDetailsV2Document(rawPrediction["prediction"]);
        rawPrediction["pages"].forEach((page) => {
            if (page.prediction !== undefined && page.prediction !== null &&
                Object.keys(page.prediction).length > 0) {
                this.pages.push(new Page(BankAccountDetailsV2Document, page, page["id"], page["orientation"]));
            }
        });
    }
}

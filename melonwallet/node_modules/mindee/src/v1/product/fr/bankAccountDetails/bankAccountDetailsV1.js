import { Inference, Page } from "../../../../v1/parsing/common/index.js";
import { BankAccountDetailsV1Document } from "./bankAccountDetailsV1Document.js";
/**
 * Bank Account Details API version 1 inference prediction.
 */
export class BankAccountDetailsV1 extends Inference {
    constructor(rawPrediction) {
        super(rawPrediction);
        /** The endpoint's name. */
        this.endpointName = "bank_account_details";
        /** The endpoint's version. */
        this.endpointVersion = "1";
        /** The document's pages. */
        this.pages = [];
        this.prediction = new BankAccountDetailsV1Document(rawPrediction["prediction"]);
        rawPrediction["pages"].forEach((page) => {
            if (page.prediction !== undefined && page.prediction !== null &&
                Object.keys(page.prediction).length > 0) {
                this.pages.push(new Page(BankAccountDetailsV1Document, page, page["id"], page["orientation"]));
            }
        });
    }
}

import { Inference, Page } from "../../../v1/parsing/common/index.js";
import { PassportV1Document } from "./passportV1Document.js";
/**
 * Passport API version 1 inference prediction.
 */
export class PassportV1 extends Inference {
    constructor(rawPrediction) {
        super(rawPrediction);
        /** The endpoint's name. */
        this.endpointName = "passport";
        /** The endpoint's version. */
        this.endpointVersion = "1";
        /** The document's pages. */
        this.pages = [];
        this.prediction = new PassportV1Document(rawPrediction["prediction"]);
        rawPrediction["pages"].forEach((page) => {
            if (page.prediction !== undefined && page.prediction !== null &&
                Object.keys(page.prediction).length > 0) {
                this.pages.push(new Page(PassportV1Document, page, page["id"], page["orientation"]));
            }
        });
    }
}

import { Inference, Page } from "../../../v1/parsing/common/index.js";
import { ResumeV1Document } from "./resumeV1Document.js";
/**
 * Resume API version 1 inference prediction.
 */
export class ResumeV1 extends Inference {
    constructor(rawPrediction) {
        super(rawPrediction);
        /** The endpoint's name. */
        this.endpointName = "resume";
        /** The endpoint's version. */
        this.endpointVersion = "1";
        /** The document's pages. */
        this.pages = [];
        this.prediction = new ResumeV1Document(rawPrediction["prediction"]);
        rawPrediction["pages"].forEach((page) => {
            if (page.prediction !== undefined && page.prediction !== null &&
                Object.keys(page.prediction).length > 0) {
                this.pages.push(new Page(ResumeV1Document, page, page["id"], page["orientation"]));
            }
        });
    }
}
